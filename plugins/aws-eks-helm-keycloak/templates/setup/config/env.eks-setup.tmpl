# EKS + Helm + Keycloak Environment Configuration
# Generated by /eks:setup on ${SETUP_DATE}
#
# Copy this file to .env.eks-setup in your project root
# DO NOT commit this file if it contains sensitive values

# ═══════════════════════════════════════════════════════════════
# AWS Configuration
# ═══════════════════════════════════════════════════════════════
AWS_REGION=${AWS_REGION}
AWS_ACCOUNT_ID=${AWS_ACCOUNT_ID}

# EKS Clusters
EKS_CLUSTER_DEV=${EKS_CLUSTER_DEV}
EKS_CLUSTER_STAGING=${EKS_CLUSTER_STAGING}
EKS_CLUSTER_PROD=${EKS_CLUSTER_PROD}

# Secrets Manager prefix for this project
SECRETS_PREFIX=${PROJECT_NAME}/

# ═══════════════════════════════════════════════════════════════
# Harness Configuration
# ═══════════════════════════════════════════════════════════════
HARNESS_BASE_URL=${HARNESS_BASE_URL:-https://app.harness.io}
HARNESS_ACCOUNT_ID=${HARNESS_ACCOUNT_ID}
HARNESS_ORG_ID=${HARNESS_ORG_ID}
HARNESS_PROJECT_ID=${HARNESS_PROJECT_ID}

# API Key - Store securely or use secrets manager
# HARNESS_API_KEY=pat.xxx.xxx

# ═══════════════════════════════════════════════════════════════
# Keycloak Configuration
# ═══════════════════════════════════════════════════════════════
KEYCLOAK_URL=${KEYCLOAK_URL}
KEYCLOAK_REALM_DEV=${KEYCLOAK_REALM_DEV:-development}
KEYCLOAK_REALM_STAGING=${KEYCLOAK_REALM_STAGING:-staging}
KEYCLOAK_REALM_PROD=${KEYCLOAK_REALM_PROD:-production}

# Admin credentials - Store securely or use secrets manager
# KEYCLOAK_ADMIN=admin
# KEYCLOAK_ADMIN_PASSWORD=xxx

# ═══════════════════════════════════════════════════════════════
# Local Development
# ═══════════════════════════════════════════════════════════════
LOCAL_APP_PORT=${LOCAL_APP_PORT:-3000}
LOCAL_KEYCLOAK_PORT=${LOCAL_KEYCLOAK_PORT:-8080}
LOCAL_LOCALSTACK_PORT=${LOCAL_LOCALSTACK_PORT:-4566}
LOCAL_REGISTRY_PORT=${LOCAL_REGISTRY_PORT:-5000}

KIND_CLUSTER_NAME=${KIND_CLUSTER_NAME:-eks-local}

# ═══════════════════════════════════════════════════════════════
# Project Settings
# ═══════════════════════════════════════════════════════════════
PROJECT_NAME=${PROJECT_NAME}
DOMAIN=${DOMAIN}
DEFAULT_BRANCH=${DEFAULT_BRANCH:-main}
