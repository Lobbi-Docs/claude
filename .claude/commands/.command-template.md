# {{command-name}}

**Usage:** `/{{command-name}} [args]`
**Context Budget:** 500 tokens max (command file only)
**Category:** {{category}}

---
context_budget: 500
strict_enforcement: true
forbidden_files:
  - commands.index.json
  - CLAUDE.full.md
  - agents.index.json
  - Any related command files
---

## Description
{{brief-description}}

## Usage
```bash
/{{command-name}} [required-args] [optional-args]
```

## Examples
```bash
/{{command-name}} example
```

## Implementation
{{command-logic-here}}

## External Resources (Load On-Demand)

**DO NOT load these in command context. Load via MCP when needed:**

- Agent definitions: `mcp__obsidian__get_file_contents("System/Claude-Instructions/Agent-Categories.md")`
- Registry data: Query `.claude/registry/commands.index.json` for metadata only
- Full documentation: Load from Obsidian vault when needed
- Related commands: Don't load unless explicitly referenced

## Context Optimization

This command follows minimal context loading:
- ✅ Loads only this file (~200-500 tokens)
- ✅ References minimal CLAUDE.md (~300 tokens)
- ❌ Does NOT load full registry
- ❌ Does NOT load full documentation
- ❌ Does NOT load all agent definitions

**Total context: ~500-800 tokens (command file + CLAUDE.md only)**

